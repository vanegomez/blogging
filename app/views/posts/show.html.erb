<div class="row">
  <h3><%= @post.title %></h3>
  <p>by: <em><strong><%= @post.author.capitalize %></strong></em></p>
  <p><%= markdown(@post.body) %></p>
</div>


<% if @post.published? %>
  <hr>

  <%= render partial: 'comments/new_comment_form' %>

  <%= render partial: 'comments/comments' %>

<% else %>
  <div class="row">
  <%= link_to "Publish", post_path(@post), method: :put %>
  </div>
<% end %>
